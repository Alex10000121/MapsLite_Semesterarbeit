# Beispiel-Umgebung für das Backend
# Pfad zur ZODB-Datei (wird automatisch erstellt, wenn nicht vorhanden)
DATABASE_FILE=./data/appdata.fs

# Erlaubte Origins (Frontend-URL), mehrere durch Komma trennen
CORS_ALLOW_ORIGINS=http://127.0.0.1:5500,http://localhost:5500
